{
  "name": "incident-calls-notification",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy",
    "lint": "eslint --ext js functions/",
    "lint:fix": "npm run lint -- --fix"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "debug": "^4.3.1",
    "qs": "^6.10.1",
    "twilio": "^3.56",
    "twilio-webhook-dispatch": "0.0.1"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-twilio": "^1.35.1",
    "twilio-run": "^3.0.1"
  },
  "engines": {
    "node": "12"
  }
}
